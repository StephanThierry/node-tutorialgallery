var http = require('http');
var fs = require('fs');
http.createServer(function (req, res) {
    console.log(req);

    var rs = fs.createReadStream(__dirname + '/testdata');
    res.writeHead(200, {'Content-Type': 'text/plain'});
    rs.pipe(res);
    rs.resume();
    res.end();

}).listen(1337);
console.log('Server running at http://127.0.0.1:1337/');